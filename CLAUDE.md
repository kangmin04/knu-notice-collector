# 공지사항 모음

## 목적
경북대학교 컴퓨터학부 학생(사용자) 입장에서, 개발자로서 도움이 될 대외활동(공모전·해커톤·프로젝트·일경험)과
대학 생활에 필요한 정보(국제처 교환학생 공지, 진로취업과 공지 등)가 여러 사이트에 흩어져 있어
매번 하나하나 확인해야 하는 불편함을 줄이기 위한 작업 공간이다.

이 디렉토리 자체는 로컬 조사·정리 과정에서 쓰는 작업 공간이며,
최종 결과물은 **노션(Notion) 데이터베이스**로 관리한다. (로컬에는 원본 코드/산출물을 쌓지 않는다.)

## 범위
- 경북대 한정이 아니라 **대구 지역** CS/이공계 학생에게 열려 있는 대외활동도 포함
- 필요 시 **전국 단위** 대학생 공모전/인턴 플랫폼도 포함 (CS/개발 관련으로 필터링)
- 학교 공식 공지: 경북대 국제처(교환학생), 진로취업과, IT대학/컴퓨터학부, SW중심대학사업단 등

## 정보 카테고리
노션 데이터베이스에서 사용하는 카테고리 기준:
- 공모전
- 해커톤
- 프로젝트
- 일경험 (인턴/근로 등)
- 교환학생 (국제처 관련)
- 진로취업 (진로취업과 공지)
- 기타

## 진행 상태
- [x] 조사 계획 수립
- [x] 정보 소스(사이트) 조사 — 경북대 공식 채널 / 대구 지역 채널 / 전국 플랫폼 (36개)
- [x] 노션 데이터베이스 생성 및 정리
- [x] Node.js 자동 수집기 구현 (경북대 공식 게시판 9곳 + 대구/전국 정적 파싱 12곳, 매일 9시 GitHub Actions 실행)
- [x] Playwright 동적 수집(SPA 사이트) 도입 — 이벤터스·데이콘 2곳 적용 (docs/adr/0002 참고)
- [ ] 사용자가 노션 통합 토큰을 발급해 GitHub Actions Secret(`NOTION_TOKEN`)으로 등록 완료

## 노션 산출물
- 정보 소스 모음 DB: https://app.notion.com/p/7d4313ef690f4733ae5cfa8ac98dd4b9 (37개 사이트)
- 새 글 피드 DB: https://app.notion.com/p/22aeaa3935f7424a9c84513ecb611ed9 (자동 수집기가 여기에 새 글을 씀)

## 자동 수집기
Node.js 스크립트(`src/collect.js`)가 경북대 공식 게시판 9곳을 매일 아침 9시(KST)에 확인해서
새 글만 "새 글 피드" DB에 추가한다. 실시간 폴링 대신 하루 1회 배치로 택한 이유:
공지/공모전은 실시간성이 필요 없고, 폴링 부하와 사이트 구조 변경 리스크를 줄이기 위함.

**GitHub Actions로 실행**(`.github/workflows/collect.yml`, cron `0 0 * * *` UTC = 09:00 KST).
로컬 launchd 대신 GitHub Actions를 쓰는 이유: 컴퓨터가 꺼져 있어도 항상 실행되고, 노션 토큰을
GitHub Secrets로 안전하게 관리할 수 있음. 러너가 매번 새 환경이라 `data/seen.json`(수집 상태)을
워크플로우가 실행 후 저장소에 커밋해서 다음 실행이 이어받도록 함 — 이 파일은 `.gitignore`에서
제외돼 있고 git으로 추적된다. GitHub repo: private, `kangmin04/knu-notice-collector`.
설정 방법은 `README.md` 참고.

자동수집 대상은 cheerio 기반 정적 파서(경북대 9곳 + 대구/전국 12곳)와 Playwright 기반
헤드리스 렌더링(SPA 사이트, 도입 중)으로 구성된다. 아키텍처/설계 결정은 `docs/architecture.md`,
`docs/adr/`를 참고. 로켓펀치(Cloudflare 차단), 대티즌/데보션(게시판 부재), 경북대 진로취업과
채용정보/인턴십(로그인 필요), DASH/DGFEZ/대구시 일자리포털(AJAX 배후 API 미확인)은 자동수집에서
제외했고 노션 "정보 소스" DB에서 수동으로만 확인한다 — 상세 사유는 `docs/PRD.md`와
`src/sites.js` 상단 주석 참고. (과거 이 문서에 "링커리어/위비티는 JS 렌더링이라 제외"라고
적혀 있었으나, 실측 결과 두 사이트 모두 현재는 서버렌더링이라 정적 파싱이 가능해 포함시켰다.)

## 참고
- `.env`는 절대 커밋하지 않는다 (노션 토큰 포함, `.gitignore`에 이미 등록됨). GitHub Actions에서는
  `.env` 대신 repo Secret `NOTION_TOKEN`을 사용한다.
